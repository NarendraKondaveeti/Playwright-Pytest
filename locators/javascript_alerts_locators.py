class JavaScriptAlertsLocators:

    JAVASCRIPT_ALERTS_LINK = (
        "a[href='/javascript_alerts']"
    )

    JS_ALERT_BUTTON = (
        "button:text('Click for JS Alert')"
    )

    JS_CONFIRM_BUTTON = (
        "button:text('Click for JS Confirm')"
    )

    JS_PROMPT_BUTTON = (
        "button:text('Click for JS Prompt')"
    )

    RESULT_MESSAGE = (
        "#result"
    )