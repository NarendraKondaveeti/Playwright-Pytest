class GeolocationLocators:

    GEOLOCATION_LINK = (
        "a[href='/geolocation']"
    )

    WHERE_AM_I_BUTTON = (
        "button:text('Where am I?')"
    )

    LATITUDE = (
        "#lat-value"
    )

    LONGITUDE = (
        "#long-value"
    )