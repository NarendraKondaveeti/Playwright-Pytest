class ShiftingContentLocators:

    SHIFTING_CONTENT_LINK = (
        "a[href='/shifting_content']"
    )

    MENU_ELEMENT_LINK = (
        "a[href='/shifting_content/menu']"
    )

    IMAGE_LINK = (
        "a[href='/shifting_content/image']"
    )

    LIST_LINK = (
        "a[href='/shifting_content/list']"
    )

    RANDOM_LINK = (
        "a[href='/shifting_content/menu?mode=random']"
    )

    PIXEL_SHIFT_LINK = (
        "a[href='/shifting_content/menu?pixel_shift=100']"
    )

    RANDOM_PIXEL_LINK = (
        "a[href='/shifting_content/menu?mode=random&pixel_shift=100']"
    )

    HOME_LINK = (
        "a[href='/']"
    )

    ABOUT_LINK = (
        "a[href='/about/']"
    )

    IMAGE_RANDOM_LINK = (
        "a[href='/shifting_content/image?mode=random']"
    )

    IMAGE_PIXEL_LINK = (
        "a[href='/shifting_content/image?pixel_shift=100']"
    )

    IMAGE_RANDOM_PIXEL_LINK = (
        "a[href='/shifting_content/image?mode=random&pixel_shift=100']"
    )

    SIMPLE_IMAGE_LINK = (
        "a[href='/shifting_content/image?image_type=simple']"
    )

    IMAGE = (
        "img[src='/img/avatar.jpg']"
    )

    IMPORTANT_TEXT = (
        "text=Important Information You're Looking For"
    )